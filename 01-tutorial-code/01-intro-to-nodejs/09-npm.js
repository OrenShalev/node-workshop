const leftPad = require('left-pad')

console.log('x', leftPad('Hello, world', 13), 'x')

/*
* npm install left-pad // not saved, use one of the below.
* npm install --save left-pad
* npm install --save-dev left-pad
 */